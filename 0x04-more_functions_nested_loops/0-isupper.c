#include "main.h"

/**
 * _isupper - to check if letter is uppercase character
 * @b: the input to be checked
 * Return: should return 1 for uppercase letter otherwise 0
 */

int _isupper(int b)

{
	if (b >= 65 && b <= 90)
	{
	return (1);
	}
	return (0);
}
